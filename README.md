# News Reader

A native Kotlin Android app that loads top headlines from NewsAPI.org.

## GitHub Actions

Add a repository secret named `NEWS_API_KEY`, then run the **Build Android APK** workflow. The generated APK is uploaded as the `news-reader-debug-apk` artifact.

No Android SDK, Gradle, or Kotlin installation is required on the local machine.
